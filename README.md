# OyasaiMusic

Minecraft 内でノートブロックの演奏を録音し、楽曲として公開・試聴・評価・プレイリスト化できる Paper プラグインです。楽曲は SQLite と独自の音源ファイルとして保存され、通常の個人再生に加えて、アイテム化したレコードをジュークボックスへ設置する環境 BGM 再生にも対応します。

## 動作環境

| 項目 | 必要なもの |
| --- | --- |
| サーバー | Paper `1.21.11` |
| Java | Java 21 |
| 必須プラグイン | FastAsyncWorldEdit (FAWE) `2.15.2` 以降 |
| 任意プラグイン | Vault と経済プラグイン、TokenManager、PlaceholderAPI、Essentials、Floodgate |

FAWE は `/record we ...` でクリップボードを読むための必須依存です。Vault が無い場合、金銭報酬の送金は行われません。TokenManager へのポイント付与は、設定したコンソールコマンドで行います。

## 導入

1. Paper と Java 21 を用意します。
2. FAWE と `OyasaiMusic-1.0.0.jar` を `plugins` フォルダへ配置します。
3. サーバーを起動します。
4. `plugins/OyasaiMusic/config.yml` を必要に応じて編集し、`/oyasaimusic reload` を実行します。

初回起動時に、データフォルダへ以下が作成されます。

```text
plugins/OyasaiMusic/
├─ config.yml             # プラグイン設定
├─ database.db            # 楽曲・ユーザー・プレイリスト等の SQLite データベース
├─ ranking_cache.json     # 日間・週間・総合ランキングのキャッシュ
├─ sound-catalog.json     # 看板用バニラ SoundEvent カタログ
├─ soundeffect/           # プラグイン通知用の録音済み効果音
│  ├─ publish_newsong.bin # 新曲公開時
│  └─ get_reward.bin      # 報酬受取時
└─ audio/
   └─ <作者名>/           # 楽曲の .bin 音源ファイル
```

統合版プレイヤー名に付く先頭の `.` は、`audio` 配下の作者ディレクトリ名から自動で除かれます。`database.db` と `audio` は楽曲本体のため、サーバー移行・更新前には必ず両方をバックアップしてください。

## コマンド

| コマンド | 説明 | 権限 |
| --- | --- | --- |
| `/musicmenu` または `/mm` | OyasaiMusic のメイン GUI を開く | `oyasaimusic.use` |
| `/mm play <ID>` | 楽曲 ID を指定して再生する | `oyasaimusic.use` |
| `/mm open <ID>` | 楽曲詳細 GUI を開き、同時に再生する | `oyasaimusic.use` |
| `/getmusicplayer [player]` | 音楽プレイヤーアイテムを配布する。プレイヤー指定は管理者／コンソールのみ | `oyasaimusic.use`、指定時は管理者 |
| `/record we grid <BPM>` | FAWE クリップボードをグリッド型として録音する | `oyasaimusic.record` |
| `/record we default` | FAWE クリップボードをレッドストーン回路型として録音する | `oyasaimusic.record` |
| `/record start <1-4>` | 実際に鳴らしたノートブロックの動的録音を開始する | `oyasaimusic.record` |
| `/record stop` | 動的録音を終了し、下書きとして保存する | `oyasaimusic.record` |
| `/demosound <SoundEvent> <パターン> [ピッチ]` | 看板用のバニラ音源を実行者だけに試聴する | `oyasaimusic.demosound` |
| `/oyasaimusic reload` | `config.yml`、SoundEvent カタログ、ランキングキャッシュを読み込み直す | `oyasaimusic.admin` |
| `/oyasaimusic update` | 日間・週間ランキングをただちに更新する | `oyasaimusic.admin` |

`/demosound` のピッチはノートブロックと同じ `0`〜`24` です。TAB 補完ではカタログに登録済みの SoundEvent、存在するパターン、ピッチ候補だけが表示されます。実行後の `ID x.x.x:n` はクリックでコピーできます。

## プレイヤー向けの使い方

### 楽曲を探して再生する

`/mm` または音楽プレイヤーアイテムを右クリックしてメインメニューを開きます。公開済み楽曲の一覧、検索、日間・週間・総合ランキング、作者一覧、プレイリストを利用できます。楽曲名の近くには楽曲 ID が表示されるため、`/mm play <ID>` や `/mm open <ID>` でも直接アクセスできます。

楽曲詳細では試聴、前／次の楽曲への移動、いいね、お気に入り、プレイリストへの追加を行えます。非公開楽曲は、作者本人と管理者だけが確認でき、いいね・プレイリスト追加の対象にはなりません。

公開直後の新曲通知は `oyasaimusic.newsong.notify` 権限を持つプレイヤーだけに届きます。`[クリックで再生]` をクリックするか、表示される `/mm open <ID>` を実行すると、詳細画面を開いて再生を開始します。

### 再生操作

GUI 下部の再生操作から、一時停止／再開、前後移動、シャッフル、ループを切り替えます。再生中の楽曲名と進捗はボスバーに表示されます。

- 曲の切替は 0.75 秒の間隔で行われます。
- プレイリストでは、1 曲ループ・リスト全体ループ・シャッフルの変更が次の遷移から即座に反映されます。
- 単曲の試聴でも、前後の公開曲へ移動できます。
- 一時停止／再開、操作不能などの短い通知はアクションバーに表示されます。重複いいね等の無効操作ではエラー音も鳴ります。

### GUI のクリックモード

左側タブで操作モードを選択します。選択中のモードは黄緑で表示されます。

| 左クリック | 右クリック |
| --- | --- |
| Shift 左クリック | Shift 右クリック |

楽曲一覧の各アイテムの lore には、各モードで実行される内容が表示されます。実行可能な操作は金色、操作のない項目は灰色です。統合版プレイヤー向けには、操作しやすいモード選択 UI も提供されます。

## 楽曲を制作・公開する

### 基本の流れ

1. 下記いずれかの方法で録音します。
2. 録音完了後に自動で開く「楽曲設定」画面で題名、BPM、レコード種類、価格、参考 URL などを設定します。保存完了後は、開いているGUIと再生中のボスバーへ直ちに反映されます。
3. 必要であればオリジナル審査へ提出します。
4. 公開を ON にします。最初の公開時だけ全体へ新曲通知が送られます。

録音された楽曲は初期状態では非公開の下書きです。公開状態と審査状態は別々に管理されます。オリジナル審査を提出した時点では「仮OK」となり、OPは審査画面で「許可」「未審査」「却下」へ切り替えます。楽曲設定で BPM を変更すると、表示値だけでなく保存済み音源の時間間隔も変更され、実際の再生速度に反映されます。

### 録音方式

| 方式 | 実行方法 | 用途 |
| --- | --- | --- |
| グリッド型 | `/record we grid <BPM>` | 等間隔に並べたノートブロックを、向きと BPM から順に読み取る |
| 回路型 | `/record we default` | リピーターの向き・遅延を含むレッドストーン回路を解析する |
| 動的録音 | `/record start <1-4>` → 演奏 → `/record stop` | 実際に鳴らしたノートブロックを時系列で記録する |

FAWE 録音では、先に対象範囲を `//copy` してください。グリッド型・回路型は、コピー元の構造（看板を含む）をワールド上に残したまま録音することを推奨します。回路型では信号源からノートブロックへの経路、リピーターの向き、遅延を考慮して解析します。

動的録音の `<1-4>` は量子化単位です。録音開始後の先頭の無音は保存時に除去されます。

### ノートブロック上の看板による上書き

ノートブロックの真上（Y+1）に看板を置くと、録音時にそのノートの設定を上書きできます。

| 看板の行 | 記述                         | 例 | 効果 |
| --- |----------------------------| --- | --- |
| 1 行目 | 音量 `0`〜`100`（`%` 可）        | `80` / `80%` | ノートの音量を上書き |
| 2 行目 | Pan `-100`〜`100` (-:左 +:右) | `-50` | 立体音響再生時の左右定位を上書き |
| 3 行目 | 四分音符を基準とする遅延               | `1/8` / `-1/16` | 正値は遅らせ、負値は早める |
| 4 行目 | バニラ SoundEvent の指定 ID      | `4.5.1:2` | ノートブロック音色を指定音源・パターンに置換 |

3 行目の `1` は四分音符 1 個分、`1/8` は八分音符 1 個分です。遅延の基準 BPM はグリッド型なら指定 BPM、回路型は基準 120 BPM、動的録音は記録セッションの基準 BPM です。

4 行目の ID は `sound-catalog.json` によって決まります。たとえば `entity.axolotl.attack` のパターン 1 は `4.5.1:1`、`ui.loom.select_pattern` のパターン 4 は `8.3.1:4` のように指定します。音量は看板 1 行目（未指定時は元ノート）、ピッチは看板下のノートブロックを使います。

楽曲設定画面でも左の「アクションモード切替」を使えます。レコード種類は左クリックで次、右クリックで前の種類へ切り替えます。統合版ではアクションモードを「左クリック」または「右クリック」に切り替えて同じ操作を行います。

## SoundEvent カタログ

`sound-catalog.json` は JAR 内の初期カタログを初回起動時にデータフォルダへ展開したものです。カタログの読み込みに失敗した場合、`/demosound` の TAB 補完や 4 行目指定は利用できません。

- 基本的にはファイルを手編集せず、そのまま使用してください。
- 変更した場合は `/oyasaimusic reload` で再読み込みします。
- 音源ごとに存在するパターンだけを `/demosound` の補完候補に出します。
- `Sound.values()` 等の Bukkit の列挙 API には依存せず、カタログ定義から解決します。

## レコードアイテムと環境 BGM

楽曲をアイテム化したレコードは、ジュークボックスへ右クリックで設置できます。独自音源のためバニラのジュークボックス内部には保存せず、設置位置をプラグインが管理します。設置時にはレコードを消費するため、クリエイティブモードでの複製も発生しません。

レコードを Shift + 右クリックすると環境 BGM 設定を開けます。再生範囲、トリガー、ループを調整できます。

| トリガー | 動作 |
| --- | --- |
| ジュークボックス | 設置と同時に再生 |
| RS 信号 | ボタン等の ON になった瞬間で再生／停止を切替。ON を保持しても再生は継続 |
| 接近 | 指定範囲にプレイヤーが入ると再生し、全員が離れると停止 |

手ぶらでジュークボックスを右クリックすると環境 BGM を停止してレコードを取り出せます。ジュークボックスを壊した場合も、登録済みの再生は解除されます。

## 報酬・統計・ランキング

- いいねは公開楽曲にのみ付けられます。重複いいねは防止されます。
- 作者がオンラインの場合、いいねを受け取ると楽曲名と送信者名を含む右上のトースト通知が届きます。
- 設定値に応じて、いいね送信者には Vault の通貨、作者にはポイントを付与します。
- 作者の未受取報酬は報酬受取バンドルに、今回の受取額・ポイント・総収入とともに表示されます。
- 視聴回数は、AFK ではなく、設定した視聴完了率を満たした再生を対象に集計します。PlaceholderAPI と Essentials がある場合は `%essentials_afk%` を使用します。
- ランキングは日間・週間・総合の楽曲／作者を表示します。管理者は `/oyasaimusic update` で即時更新できます。

## 設定

主な設定は `plugins/OyasaiMusic/config.yml` にあります。

| 設定 | 説明 |
| --- | --- |
| `storage.database-file` | SQLite データベースのファイル名 |
| `storage.audio-directory` | 音源ファイルの保存先 |
| `recording.default-price` | 新規楽曲の既定価格 |
| `recording.default-record-material` | 新規楽曲の既定レコード素材 |
| `recording.dynamic-record-radius` | 動的録音でノートを拾う最大半径 |
| `playback.default-mode` | `default` または `positional`。既定の再生方式 |
| `playback.view-*` | 視聴数に数える回数上限・完了率・ポイント換算 |
| `economy.like-reward-*` | いいね時の通貨／ポイント報酬 |
| `economy.record-sale-author-share` | レコード売上の作者還元率 |
| `economy.points-command` | ポイントを付与するコンソールコマンド |
| `bedrock.name-prefix` | 統合版プレイヤー名を判定する接頭辞 |

TokenManager を使う場合の既定値は以下です。

```yaml
economy:
  points-command: "tokenmanager add %player% %points%"
```

`%player%` は受取プレイヤー名、`%points%` と `%amount%` はポイント値に展開されます。設定を変えた後は `/oyasaimusic reload` を実行してください。

## プラグイン効果音

新曲公開時と報酬受取時には、録音済みの `.bin` 効果音を再生します。初回起動で `soundeffect` フォルダへ標準音源が展開されます。将来の拡張用に、効果音はこのフォルダから読み込む構造です。同じファイル名の有効な OYMB 形式 `.bin` に差し替えると、次回の該当通知から任意の効果音を利用できます。

## 権限一覧

| 権限 | 既定 | 内容 |
| --- |----| --- |
| `oyasaimusic.use` | 未定 | メニュー、再生、音楽プレイヤーの使用 |
| `oyasaimusic.record` | 未定 | すべての録音コマンド |
| `oyasaimusic.demosound` | 未定 | `/demosound` |
| `oyasaimusic.admin` | OP | 管理コマンド、審査・管理用 GUI、他人への音楽プレイヤー配布 |
| `oyasaimusic.newsong.notify` | 未定 | 新曲公開のチャット通知・効果音の受信 |

## 運用上の注意

- 公開楽曲だけが一般の一覧・検索・ランキング・いいね・プレイリスト追加の対象です。
- 楽曲を削除すると、そのIDは次回以降の録音完了時に最も小さい欠番から再利用されます。
- `/oyasaimusic reload` は設定・カタログ等を読み直しますが、進行中の録音や再生を再構築するためのコマンドではありません。設定変更は人の少ない時間帯に行うことを推奨します。
- データベースファイルだけを移動して `audio` を移動しないと、楽曲情報は残っていても再生できません。
- `sound-catalog.json` が削除・破損した場合は、バックアップから戻すか、プラグイン JAR 内の同名ファイルをデータフォルダへ復元してから `/oyasaimusic reload` を実行してください。
